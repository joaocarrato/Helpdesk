import {ImageSourcePropType} from 'react-native';

const imageBackground:
  | ImageSourcePropType
  | undefined = require('../assets/images/backgroundImage.png');

export const imageUtils = {imageBackground};
