import React from 'react';

import {Container, Text} from '@components';
import {AppStackScreenProps} from '@routes';

export function CreateNewTechnicianScreen({}: AppStackScreenProps<'CreateNewTechnicianScreen'>) {
  return (
    <Container>
      <Text>CreateNewTechnicianScreen</Text>
    </Container>
  );
}
